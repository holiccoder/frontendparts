{
    "caption": {
        "type": "string",
        "default": "",
        "description": "Small caption above the row. Hidden when empty."
    },
    "logos": {
        "type": "array",
        "default": [],
        "description": "Wordmarks to display; each item is an object with a `name` string."
    }
}
