{
    "brand": {
        "type": "string",
        "default": "Acme",
        "description": "Brand name shown next to the logo mark."
    },
    "links": {
        "type": "array",
        "default": [],
        "description": "Navigation links; each item is an object with `label` and `href` strings."
    },
    "ctaLabel": {
        "type": "string",
        "default": "Get started",
        "description": "Call-to-action button label."
    },
    "ctaHref": {
        "type": "url",
        "default": "#",
        "description": "Call-to-action link target."
    }
}
