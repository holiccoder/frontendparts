{
    "children": {
        "type": "object",
        "default": {},
        "description": "Child slices keyed by child slug; `section-title-01` takes one Section Title prop object, `testimonial-card-01` takes an array of three Testimonial Card prop objects."
    }
}
