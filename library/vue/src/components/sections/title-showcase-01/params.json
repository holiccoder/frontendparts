{
    "children": {
        "type": "object",
        "default": {},
        "description": "Child slices keyed by child slug; `section-title-01` takes an array of two Section Title prop objects."
    }
}
